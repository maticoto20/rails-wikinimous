<div class="container">
  <% @articles.each do |article| %>
    <div class="article">
      <h1><%= link_to article.title, article_path(article) %></h1>
      <p><%= article.content %></p>
    </div>
  <% end %>
</div>
